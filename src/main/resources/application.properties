# Servidor
server.port=8080

# Base de datos MySQL (XAMPP)
spring.datasource.url=jdbc:mysql://localhost:3307/facturacion_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
#spring.jpa.hibernate.ddl-auto=validate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000

# Swagger / OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

# Logging
logging.level.com.empresa.sistema=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.encoding=UTF-8

# URL base de la aplicación (se usa en correos para el botón "Ver Documento")
app.base-url=http://localhost:8080

# ── Correo electrónico ───────────────────────────────────────────
# Activar/desactivar envío real de correos (false = NoOp silencioso)
app.mail.enabled=true

# Configuración SMTP (descomentar y completar cuando app.mail.enabled=true)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=andresparedesg.ap@gmail.com
spring.mail.password=qnky plpp dxgl opcq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.default-encoding=UTF-8
